#lang info

(define collection 'multi)
(define deps '("base" "string-constants-lib"))

(define pkg-desc "LGPL string constants to support internationalization, especially in DrRacket")

(define pkg-authors '(robby))

(define version "1.1")
